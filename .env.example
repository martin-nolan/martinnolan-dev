# =============================================================================
# MARTIN NOLAN PORTFOLIO - ENVIRONMENT CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# AI CHAT CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Get your GitHub Models token from: https://github.com/settings/tokens
# Requires access to GitHub Models (may need waitlist approval)
GITHUB_TOKEN=your_github_models_token_here

# -----------------------------------------------------------------------------
# CONTACT FORM CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# EmailJS setup for contact form functionality
# Sign up at: https://www.emailjs.com/
NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_emailjs_service_id
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_emailjs_public_key

# -----------------------------------------------------------------------------
# CMS CONFIGURATION (Optional - Future Enhancement)
# -----------------------------------------------------------------------------
# Strapi CMS for dynamic content management
# Set up your own Strapi instance or use hosted service
NEXT_PUBLIC_STRAPI_API_URL=https://your-strapi-instance.com/api
STRAPI_API_TOKEN=your_strapi_api_token

# Webhook secret for automatic rebuilds when content changes
WEBHOOK_SECRET=your_webhook_secret_for_rebuilds

# -----------------------------------------------------------------------------
# DEVELOPMENT CONFIGURATION
# -----------------------------------------------------------------------------
# Set to "true" to validate environment variables on startup
VALIDATE_ENV=false

# Node environment (automatically set by deployment platforms)
NODE_ENV=development
