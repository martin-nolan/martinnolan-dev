# Local Development

This file is a placeholder for target-repo commands.

After copying the harness into another repository, replace this file with the real setup, run, test, and validation commands for that repository.

## Expected Sections

- prerequisites
- install/setup
- run locally
- lint/type-check/test
- build/package
- documentation validation
- release or deployment checks, if applicable

## Command Rule

Do not invent commands for the harness. The target repo's package scripts, task runner, CI workflow, or documented command surface should be the source of truth.

If the repo has no standard command for a required check, document that gap and add one deliberately.
