# Production Deployment Configuration
# Copy this to your deployment platform's environment variables

# =============================================================================
# REQUIRED ENVIRONMENT VARIABLES FOR PRODUCTION
# =============================================================================

# AI Chat Configuration (Required)
GITHUB_TOKEN=your_github_models_token_here

# CMS Configuration (Required)
NEXT_PUBLIC_STRAPI_API_URL=https://your-strapi-instance.com/api
STRAPI_API_TOKEN=your_strapi_api_token

# Optional: Override default GitHub Models settings
GITHUB_MODELS_ENDPOINT=https://models.github.ai/inference
GITHUB_MODEL_ID=openai/gpt-4.1

# Contact Form Configuration (Optional)
NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_emailjs_service_id
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_emailjs_public_key

# Webhook Configuration (Optional)
WEBHOOK_SECRET=your_webhook_secret_for_rebuilds

# Production Settings
NODE_ENV=production

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Never commit these values to version control
# 2. Use your deployment platform's environment variable settings
# 3. Rotate API tokens regularly
# 4. Use least-privilege access for all tokens
# 5. Monitor for any exposed credentials in logs

# =============================================================================
# DEPLOYMENT PLATFORM SPECIFIC NOTES
# =============================================================================
# 
# Vercel:
# - Add these in your Vercel dashboard under Settings > Environment Variables
# - Use different values for Preview vs Production deployments
#
# Netlify:
# - Add these in Site Settings > Environment Variables
# - Enable build hooks for CMS updates
#
# Other platforms:
# - Ensure all NEXT_PUBLIC_ variables are available at build time
# - Server-side variables (without NEXT_PUBLIC_) should be secure
